# Synthetic sample images

Generated by `app/sample_data.py` from drawing primitives. No real patient data is involved. They exist to exercise the pipeline and the UI, and are **not** evidence that the placeholder model works on real skin.

- `foot_urgent.png` — module `foot`, expected grade `urgent`. Open wound bed with slough — breakdown of the surface itself.
- `foot_dark_area.png` — module `foot`, expected grade `review`. Discrete dark area. Routed for inspection, NOT called necrotic.
- `foot_shadow_only.png` — module `foot`, expected grade `no_flag`. Healthy skin with shadow creases — must not alarm.
- `foot_clean.png` — module `foot`, expected grade `no_flag`. Intact-appearing skin, no surface red flag.
- `skin_urgent.png` — module `skin`, expected grade `urgent`. Irregular, asymmetric, multi-coloured pigmented lesion.
- `skin_clean.png` — module `skin`, expected grade `no_flag`. Uniform skin, no discrete lesion.
- `eye_urgent.png` — module `eye`, expected grade `urgent`. Yellow sclera — possible jaundice on the image surface.
- `eye_clean.png` — module `eye`, expected grade `no_flag`. White sclera, no anterior-surface flag.
- `eye_anisocoria.png` — module `eye`, expected grade `urgent`. Pupils differ by ~2.6 mm — routing flag, cause not determined.
- `eye_pupils_equal.png` — module `eye`, expected grade `no_flag`. Pupils within 0.2 mm of each other in this lighting.
- `face_cyanosis.png` — module `face`, expected grade `urgent`. Lips read blue against the cheek — possible central cyanosis.
- `face_jaundice.png` — module `face`, expected grade `urgent`. Sclera reads yellow against the in-frame white reference.
- `face_normal.png` — module `face`, expected grade `no_flag`. No colour flag. Does not exclude hypoxaemia or anaemia.
- `injury_urgent.png` — module `injury`, expected grade `urgent`. Visible contour deformity — routing red flag only.
- `injury_review.png` — module `injury`, expected grade `review`. Extensive bruising — routing red flag only.
- `injury_clean.png` — module `injury`, expected grade `no_flag`. No external red flag. Does NOT exclude internal injury.
- `quality_blurred.png` — module `foot`, expected grade `quality rejection`. Out of focus — must be rejected by the quality gate.
- `quality_dark.png` — module `foot`, expected grade `quality rejection`. Under-exposed — must be rejected by the quality gate.
- `quality_small.png` — module `foot`, expected grade `quality rejection`. Below minimum resolution — must be rejected by the quality gate.
